{
  "name": "test-report-generation",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node -e \"const r = require('./src/parsers/jest-vitest'); const fs = require('fs'); const c = fs.readFileSync('./tests/fixtures/jest-result.json', 'utf-8'); const result = r.parseJestVitestJson(c); console.log('Parsed:', result.summary.total, 'test cases');\"",
    "test:junit": "node -e \"const r = require('./src/parsers/junit-xml'); const fs = require('fs'); const c = fs.readFileSync('./tests/fixtures/junit-result.xml', 'utf-8'); const result = r.parseJUnitXml(c); console.log('Parsed:', result.summary.total, 'test cases');\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
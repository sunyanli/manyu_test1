import React from 'react';
import ReactDOM from 'react-dom/client';
import AlgorithmDashboard from './AlgorithmDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<AlgorithmDashboard />);